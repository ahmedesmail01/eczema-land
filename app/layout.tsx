import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

const customMonadi = localFont({
  src: [
    {
      path: "./fonts/ArbFONTS-Monadi.ttf",
      weight: "400",
      style: "normal",
    },
  ],
  variable: "--font-monadi",
});

export const metadata: Metadata = {
  title: "الصدفية و الاكزيما",
  description: "Generated by MTN-Development",
};

const cairo = localFont({
  src: "./fonts/Cairo-Regular.ttf",
  variable: "--font-cairo",
  weight: "400",
  style: "normal",
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} ${customMonadi.variable} ${cairo.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
